#
# wm independent hotkeys
#

# brightness control
XF86MonBrightness{Up,Down}
	             xbacklight{ --inc 10, --dec 10}
	
# volume control 
XF86AudioRaiseVolume
              pamixer -i 5
              # amixer set Master 5%+
              # pactl set-sink-volume @DEFAULT_SINK@ +5%

XF86AudioLowerVolume
              pamixer -d 5 
              # amixer set Master 5%-
              # pactl set-sink-volume @DEFAULT_SINK@ -5%

XF86AudioMute
         pamixer -t
         # amixer set Master toggle
         # pactl set-sink-mute @DEFAULT_SINK@ toggle

# mic control
XF86AudioMicMute
            pactl set-source-mute @DEFAULT_SOURCE@ toggle

# media player control 
XF86Audio{Prev,Next}
         playerctl {previous,next}

XF86AudioPlay 
         playerctl play-pause

# switch layout
# super + n
        # ~/.local/bin/bspwm/layout
        ## bsp-layout next --layouts tall,tiled

# hide unhide bar
super + u
        ~/.local/bin/statusbar/toggle-polybar
        # polybar-msg cmd toggle | bspc config top_padding 0

# hide all windows
super + d
        while bspc node any.hidden.window -g hidden=off; do false; done && while bspc node 'any.!hidden.window' -g hidden=on; do :; done

# pywal
super + alt + o
              ~/.local/bin/pywal

# wallch
super + alt + p
              ~/.local/bin/wallch

# temp clip
super + alt + t
              ~/.local/bin/dm-script/tclip

# display clip
super + alt + d
              ~/.local/bin/dm-script/dclip

# bluetooth
alt + F1
      ~/.local/bin/btch    		    
	     
# vol down
alt + F2
      pamixer -d 5 
      # amixer set Master 5%-
      # pactl set-sink-volume @DEFAULT_SINK@ -5%

# vol up
alt + F3
      pamixer -i 5 
      # amixer set Master 5%+
      # pactl set-sink-volume @DEFAULT_SINK@ +5%

# mute
alt + F4
      pamixer -t
      # amixer set Master toggle         
      # pactl set-sink-mute @DEFAULT_SINK@ toggle

 # system_action
super + F4
        ~/.local/bin/dm-script/sys-action
	 
# switch audio
alt + F5       
      ~/.local/bin/audiosw    

# previous
alt + F6 
      playerctl previous   
	
# play, pause   
super + F7
        playerctl play-pause
  
# spotify play pause
alt + F7
      dbus-send --print-reply --dest=org.mpris.MediaPlayer2.spotify /org/mpris/MediaPlayer2 org.mpris.MediaPlayer2.Player.PlayPause

# next
alt + F8
      playerctl next
       
# wifi
alt + F9
      ~/.local/bin/dm-script/wifi             

# screenshot
Print
    maim | tee $(xdg-user-dir PICTURES)/$(date +%I:%M%b_%d)-Ful.png | xclip -sel c -t image/png | notify-send -t 800 "Screenshot Taken 📎"
                 
# ss select   
shift + Print            
        maim -s | tee $(xdg-user-dir PICTURES)/$(date +%I:%M%b_%d)-Sel.png | xclip -sel c -t image/png | notify-send -t 800 "Selected Screenshot 📎" 

# ss window
alt + Print 
	    maim -i $(xdotool getactivewindow) $(xdg-user-dir PICTURES)/$(date +%I:%M%b_%d)-Win.png | notify-send -t 800 "Window Screenshot Taken"
	                           
# chrome
alt + b
      $BROWSER
        
# rofi
alt + 0
      rofi -show drun -show-icons

# dmenu
super + @space 
        dmenu_run

# spotify
alt + s
      spotify-launcher
      # LD_PRELOAD=/usr/local/lib/spotify-adblock.so spotify %U                 
                        
# telegram
alt + t
      telegram-desktop

# terminal
super + Return
        kitty 
           
# thunar
super + e
        thunar

# calc
ctrl + alt + k
             mate-calculator

# screenlock
ctrl + alt + l
             betterlockscreen -l blur
	     
# kill ps
ctrl + alt + Delete
             ~/.local/bin/dm-script/kil	     


#
# bspwm hotkeys
#

# make sxhkd reload its configuration files
super + Escape
	      notify-send -t 500 "reloading sxhkd!"; pkill -USR1 -x sxhkd
	
# quit/restart bspwm
super + alt + {q,r}
      	bspc {quit,wm -r}

# close and kill
super + {_,shift + }q
        bspc node -{c,k}

# alternate between the tiled and monocle layout
super + m
      	bspc desktop -l next

# swap the current node and the biggest window
super + g
      	bspc node -s biggest.window
	
# send the newest marked node to the newest preselected node
super + y
      	bspc node newest.marked.local -n newest.!automatic.local

#
# state/flags
#

# increase\decrease window gap on focused desktop
super + {minus,equal}
        bspc config -d focused window_gap $((`bspc config -d focused window_gap` {-, +} 5))

super + {s,f}
        {bspc config pointer_follows_focus true; \
        bspc node -t $(bspc query -N -n -n .tiled >/dev/null && echo floating || echo tiled); \
        bspc node -f; \
        bspc config pointer_follows_focus false,bspc config pointer_follows_focus true; \
        bspc node -t $(bspc query -N -n -n .tiled >/dev/null && echo fullscreen || echo tiled); \
        bspc node -f; \
        bspc config pointer_follows_focus false}

# set the window state
super + {p,shift + p}
       	bspc node -t {tiled,pseudo_tiled}

# set the node flags
super + ctrl + {m,x,s,p}
	      bspc node -g {marked,locked,sticky,private}

#
# focus/swap
#

# focus the next/previous window in the current desktop
alt + {_,shift + }Tab
     	bspc node -f {next,prev}.local.!hidden.window

# focus the older or newer node in the focus history
super + {o,i}
      	bspc wm -h off; \
	      bspc node {older,newer} -f; \
      	bspc wm -h on

# focus the next/previous desktop in the current monitor
super + bracket{left,right}
	      bspc desktop -f {prev,next}.local

# focus the last node/desktop
super + {grave,Tab}
	      bspc {node,desktop} -f last

# focus the node in the given direction
super + {_,shift + }{h,j,k,l}
	      bspc node -{f,s} {west,south,north,east}

# focus or send to the given desktop
super + {_,shift + }{1-9,0}
	      bspc {desktop -f,node -d} '^{1-9,10}'
	
# focus the node for the given path jump
super + {p,b,comma,period}
       	bspc node -f @{parent,brother,first,second}

#
# preselect
#

# preselect the direction
super + ctrl + {h,j,k,l}
	      bspc node -p {west,south,north,east}

# preselect the ratio
super + ctrl + {1-9}
       	bspc node -o 0.{1-9}

# cancel the preselection for the focused node
super + ctrl + space
	      bspc node -p cancel

# cancel the preselection for the focused desktop
super + ctrl + shift + space
        bspc query -N -d | xargs -I id -n 1 bspc node id -p cancel

#
# move/resize
#

# expand a window by moving one of its side outward
super + alt + {h,j,k,l}
	      bspc node -z {left -160 0,bottom 0 160,top 0 -160,right 160 0}

# contract a window by moving one of its side inward
super + alt + shift + {h,j,k,l}
	      bspc node -z {right -160 0,top 0 160,bottom 0 -160,left 160 0}

# move a floating window
super + {Left,Down,Up,Right}
	      bspc node -v {-160 0,0 160,0 -160,160 0}
