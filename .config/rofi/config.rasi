configuration {
    modes: "drun,run";
    font:  "Sans 11";
}

* {
    foreground: #f8f8f2;
    background: #181825;
    selected-normal-background:  #2d3f76;
    selected-active-background:  #ffb86c;
    alternate-normal-background: var(background);
    alternate-active-background: var(background);
}

window {
    width: 32%;
}

inputbar {
    padding:  10px;
    children: [ entry ];
}

message {
    border: 2px solid 0px 0px;
}

entry {
    placeholder: "";
}

listview {
    lines:     6;
    scrollbar: false;
    border:    2px solid 0px 0px;
}

element {
    padding: 10px ;
}

element-text {
    highlight: #0db9d7;
}
