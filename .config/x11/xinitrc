#!/bin/sh

if [ -f "$XDG_CONFIG_HOME/.config/x11/xprofile" ]; then
	. "$XDG_CONFIG_HOME/.config/x11/xprofile"
else
	. "$HOME/.xprofile"
fi

session=${1:-bspwm}
case $session in
bspwm) bspwm ;;
gnome) gnome-session ;;
*) exec $1 ;;
esac
