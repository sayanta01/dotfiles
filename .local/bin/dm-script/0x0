#!/bin/sh

# file=$(find "$HOME" -type f | dmenu -i -l 20)
file=$(fd -H -t f | dmenu -i -l 20)

if [ -n "$selected_pid" ]; then
	# curl -F "file=@$file" ix.io | xclip -sel c
	curl -F "file=@$file" 0x0.st | xclip -sel c
	notify-send "0x0 "
fi
