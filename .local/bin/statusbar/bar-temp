#!/bin/sh

cpu_temp=$(sensors | awk '/Core 0/ {print $3}' | cut -c 2- | cut -b 1,2)

echo ""🌡"$cpu_temp"°C""

# for click module
# notify-send -t 990 "$(echo 菉) $(nvidia-smi --query-gpu=temperature.gpu --format=csv,noheader)$(echo °C)"
