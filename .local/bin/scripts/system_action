#!/bin/sh

case "$(readlink -f /sbin/init)" in
	*systemd*) ctl='systemctl' ;;
	*) ctl='loginctl' ;;
esac

case "$(printf "Reboot\n Shutdown" | dmenu -i -p '>_ ')" in
	' Reboot') $ctl reboot ;;
	' Shutdown') $ctl poweroff ;;
	*) exit 1 ;;
esac
